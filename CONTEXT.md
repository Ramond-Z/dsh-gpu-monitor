# dsh-gpu-monitor

GPU 用量实时监控：DeepSeek Harness 侧边栏插件 + 独立 sidecar 进程 + Electron 菜单栏应用，共用同一套核心。监控本机与 `~/.ssh/config` 中所有可用 GPU server，每台机器纵向堆叠成一个分组，每张 GPU 一个方块。

## Language

**GPU 分组（分组）**:
一台被监控的机器（本机或经 SSH 的远程机），在面板里纵向堆叠成一组。分组的稳定身份是状态快照中该机的 `host` 字段；分组顺序也以它为主键。
_Avoid_: server（歧义：既是"机器"又是"HTTP 服务"）、主机

**GPU 方块（方块）**:
分组内单张 GPU 的渲染单元：填充高度 = 显存占用，填充颜色 = 功率占比（绿 <40% < 黄 <70% < 橙 <90% < 红），悬停显示该卡的计算进程。
_Avoid_: 卡片、tile

**计算进程**:
某张 GPU 上的进程：属主 / PID / 占用显存 / 命令行。悬停方块可见，触摸设备点按查看。
_Avoid_: 任务、job

**状态快照（快照）**:
一轮查询完成后发布的完整状态：`{ok, at, source, servers[], order}`。它是引擎的唯一输出，也是所有数据源共用的线格式。
_Avoid_: status、payload

**分组顺序（顺序）**:
分组在面板中的上下次序，格式 `{o: string[], t: 时间戳}`。宿主侧持久化、跨浏览器共享"上次退出时的顺序"；浏览器另有 localStorage 缓存。
_Avoid_: 排序（保留给"把机器排进顺序"这一动作）

**顺序协议**:
分组顺序的仲裁规则：(1) 调和——保留已知 id 的顺序、新机器追加末尾；(2) 时间戳大的胜出；(3) 本地缓存比服务端新时回同步。唯一权威在引擎；客户端信任线上的结果，仅在 sidecar 离线时用本地缓存兜底。

**数据源**:
客户端获取状态快照的来源，按优先级：同源桥 → 宿主 `/gpu/status` → 显式 sidecar 绝对地址。任一成功即用；全部失败则沿用上次快照并显示警告条。
_Avoid_: 后端、服务端

**同源桥（JSON 桥）**:
sidecar 把状态快照写入 DSH 前端 dist 下的 `gpu-status.json`，浏览器同源读取、无 CORS。写入路径由 sidecar 自动探测。
_Avoid_: 代理、bridge 直译

**探测（discovery）**:
周期扫描 `~/.ssh/config` 候选主机，判定"可用"= 免密登录成功且 `nvidia-smi` 至少报 1 张 GPU。瞬时失败不立即移除，连续失败达到上限才清出列表。
_Avoid_: 扫描（与配置文件扫描混淆）

**查询（query）**:
对一台机器执行 `nvidia-smi` / `ps` 并解析，产出该分组的数据（含每卡计算进程）。单台超时只影响该台，不拖垮整轮。

**空闲 GPU**:
利用率 <5% 且显存占用 <1G 且无计算进程的 GPU；用于顶栏与分组表头的"x/y 张 GPU"统计。
